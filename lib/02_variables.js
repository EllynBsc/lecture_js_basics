// DECLARING VARIABLES
// old way ES5 version of js
  // var age = 18;
  // console.log(age);

  // age = age + 1;
  // console.log(age);

// ES6 : differenciation between variables we can re-assign and the ones we can't re-assigned
  // variable we can re-assign: LET
  // let age = 18;
  // console.log(age);
  // age += 1 ;
  // console.log(age);

  // variable you don;t want to re-assign: CONST
  // const name = 'Marta';
  // console.log(name);

// let age; // undefined is there to declare a variable without assigning it(works only with let)
// console.log(age);

let name = null; //null is like nil is ruby
console.log(name);

let firstName = 'Xavier'; //variables lowerCamelCase
